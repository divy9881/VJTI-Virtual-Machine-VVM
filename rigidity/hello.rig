function main() do
  s := '{1: 1, \'test\': \'test\', 2.0: 2.0}';
  m := string_to_map(s);
  flag := '';
  if map_has(m, 3) then
    flag := 'True'
  else
    flag := 'False'
  end;
  ans := update_contract_output(flag);
  return ans
end;
ans := main();
return ans